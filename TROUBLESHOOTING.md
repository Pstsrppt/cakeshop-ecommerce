# ปัญหาที่พบระหว่างพัฒนา และวิธีแก้ไข

เอกสารนี้สรุปอุปสรรคจริงที่เจอระหว่างพัฒนาโปรเจกต์ CakeShop พร้อมสาเหตุและวิธีแก้ — เก็บไว้เพื่อใช้อธิบายกระบวนการทำงานจริง (ไม่ใช่แค่ผลลัพธ์สุดท้าย) และเป็นบันทึกกันลืมสำหรับตัวเองในอนาคต

---

## 1. Process เก่าค้างพอร์ต ทำให้รันโปรเจกต์ใหม่ไม่ได้

**อาการ:** `dotnet run` แล้วเจอ `Failed to bind to address http://127.0.0.1:5000: address already in use`

**สาเหตุ:** มี instance เก่าของแอปตัวเองที่เคยรันไว้ยังไม่ถูกปิด ค้างจับพอร์ต 5000 อยู่

**วิธีแก้:** หาโปรเซสที่ถือพอร์ตอยู่ (`Get-NetTCPConnection -LocalPort 5000`) แล้วปิดทิ้งก่อนรันใหม่ — ถ้ารันเองในเทอร์มินัลและใช้เครื่องมือ AI ช่วยรันพร้อมกันด้วย ต้องตกลงกันว่าใครรันเป็นหลัก ไม่งั้นจะชนพอร์ตกันเอง

---

## 2. เชื่อมต่อ MySQL ไม่ได้ (`Access denied`) ทั้งที่รหัสผ่านถูก — รอบที่ 1

**อาการ:** `MySqlException: Access denied for user 'root'@'localhost' (using password: YES)` ทั้งที่ login ผ่าน MySQL client ปกติได้

**สาเหตุ:** MySQL 8+/9 ใช้ authentication plugin เริ่มต้นเป็น `caching_sha2_password` ซึ่งต้องมี SSL หรือต้องอนุญาตให้ client ดึง RSA public key มาเข้ารหัสรหัสผ่านก่อนถึงจะทำ full authentication ได้ ถ้าไม่เปิดอนุญาตไว้ MySqlConnector จะรายงาน error เป็น "Access denied" ทั้งที่จริงๆ เป็นปัญหาการเจรจา protocol ไม่ใช่รหัสผ่านผิด

**วิธีแก้:** เพิ่ม `AllowPublicKeyRetrieval=True` ต่อท้าย connection string

---

## 3. รหัสผ่านฐานข้อมูลฝังอยู่ในซอร์สโค้ดตรงๆ

**อาการ:** ก่อน push ขึ้น GitHub พบว่ารหัสผ่าน MySQL จริงถูกเขียนตรงๆ ในไฟล์ `.cs` หลายจุด

**ความเสี่ยง:** ถ้า commit ขึ้นไปแบบนี้ รหัสผ่านจะฝังอยู่ใน git history ตลอดไป ต่อให้ลบออกทีหลังก็ยังย้อนดูได้จาก commit เก่า

**วิธีแก้:** ย้ายรหัสผ่านจริงไปไว้ใน `appsettings.Development.json` แล้วใส่ไฟล์นี้ใน `.gitignore` ส่วนไฟล์ที่ commit ขึ้นจริง (`appsettings.json`, `Csi402dbContext.cs`) ให้ใช้ placeholder แทน

---

## 4. เอกสารวิเคราะห์ระบบ (DFD/Flowchart) ไม่ตรงกับโค้ดจริง

**อาการ:** ตรวจ Data Flow Diagram และ Flowchart เทียบกับโค้ดจริง เจอจุดคลาดเคลื่อนหลายจุด เช่น:
- อ้างว่าสินค้ามี "หมวดหมู่" (category) ทั้งที่ตาราง Products ไม่มีคอลัมน์นี้เลย
- อ้างว่ามีการเช็ค username ซ้ำตอนสมัครสมาชิก ทั้งที่โค้ดจริงยังไม่มี logic นี้ (มีแค่ unique constraint ระดับฐานข้อมูล ถ้าซ้ำจริงจะเกิด error 500 ตรงๆ)
- ตาราง StockLogs ในเอกสารระบุว่ามีฟิลด์ "เหตุผล" และ "ผู้ดำเนินการ" ทั้งที่ schema จริงไม่มี

**บทเรียน:** เอกสารที่เขียนจากความจำ/ความตั้งใจตอนออกแบบ มักจะเพี้ยนจากสิ่งที่โค้ดทำจริงหลังผ่านการแก้ไขหลายรอบ ต้องไล่เช็คกับโค้ดจริงทีละบรรทัดแทนการเชื่อเอกสารเฉยๆ

**วิธีแก้:** แก้เอกสารให้ตรงกับโค้ดในจุดที่เป็นแค่คำอธิบาย และ**แก้โค้ดเพิ่ม**ในจุดที่พฤติกรรมที่เอกสารบอกไว้สมเหตุสมผลกว่า (เช่น เพิ่มการเช็ค username ซ้ำจริงใน `UserController.Register` ให้ตรงกับที่ผังบอกไว้)

---

## 5. ข้อมูลตัวอย่างในฐานข้อมูลไม่สมจริง เตรียมไม่พร้อมสำหรับถ่ายภาพพอร์ตโฟลิโอ

**อาการที่พบ:**
- โค้ดโปรโมชัน `SUMMER20` มีวันหมดอายุผิดเพี้ยนเป็นปี 5012
- สินค้า "เค้กงานแต่ง" ซ้ำกันจริง **3 รายการ** (ไม่ใช่ 2 ตามที่คาดไว้ตอนแรก) หนึ่งในนั้นสต็อกติดลบ (-17) จากบั๊กตอนทดสอบ
- ไฟล์รูปภาพของสินค้าที่ซ้ำกันหายไปจากดิสก์จริง ทำให้เว็บ fallback ไปแสดงรูปเค้กหน้าตลก (`no-image.png`) แทนโดยไม่รู้ตัว
- บัญชีลูกค้าเกือบทุกออเดอร์เป็นบัญชีทดสอบเดียวกัน (`golf_lnwza007`) ทำให้หน้าจัดการออเดอร์ดูซ้ำซาก ไม่สมจริง
- กราฟยอดขาย 7 วันย้อนหลังมีข้อมูลอยู่วันเดียว เพราะออเดอร์ส่วนใหญ่ไม่ได้กระจายอยู่ในช่วง 7 วันล่าสุด

**วิธีแก้:** เขียน SQL script แก้ข้อมูลแบบ data-only (ไม่แตะ schema/password) พร้อม backup ฐานข้อมูลก่อนรันทุกครั้งด้วย `mysqldump`

**สิ่งที่ต้องระวังเพิ่ม:** สินค้าที่ซ้ำกันบางรายการมีคำสั่งซื้อจริงอ้างอิงอยู่ (Foreign Key) **ลบทิ้งตรงๆ ไม่ได้** เพราะจะทำให้ประวัติคำสั่งซื้อพัง ต้อง "เปลี่ยนชื่อสินค้า" แทนการลบแถวสำหรับรายการที่มีการอ้างอิง

---

## 6. รันสคริปต์ SQL ผ่าน PowerShell แล้วภาษาไทยเสียหายจริงในฐานข้อมูล

**อาการ:** หลังรัน SQL script ผ่าน `Get-Content -Raw file.sql | mysql ...` ใน PowerShell ข้อความภาษาไทยที่เพิ่งเพิ่มใหม่แสดงผลเป็นตัวอักษรเพี้ยน (mojibake)

**จุดสำคัญ:** ตรวจสอบแล้วพบว่า**ไม่ใช่แค่จอแสดงผลเพี้ยน แต่ข้อมูลถูกเข้ารหัสซ้อน (double UTF-8 encoding) จริงในฐานข้อมูล** — พิสูจน์ได้จากการเทียบ `CHAR_LENGTH()` กับ `LENGTH()` (byte length) ของข้อความ ถ้าภาษาไทยเข้ารหัส UTF-8 ถูกต้อง ต้องได้ 3 ไบต์ต่อ 1 ตัวอักษรพอดี

**สาเหตุ:** PowerShell แปลง encoding ของข้อความระหว่างส่งผ่าน pipe ไปยังโปรเซสภายนอก (`mysql.exe`) ทำให้ไบต์ UTF-8 ต้นฉบับถูกตีความผิดแล้วเข้ารหัสซ้ำ

**วิธีแก้:** เปลี่ยนไปรันผ่าน Bash แทน (ใช้ `<` redirection แบบ native ของ POSIX shell ซึ่งส่งไบต์ตรงๆ ไม่ผ่านการแปลง encoding แบบ PowerShell) แล้วเขียน UPDATE แก้ไขข้อมูลที่เสียหายซ้ำอีกรอบ พร้อมตรวจสอบระดับไบต์ยืนยันก่อนปิดงาน

---

## 7. เว็บใช้งานไม่ได้ทั้งที่ข้อมูลถูกต้องหมดแล้ว — ปัญหาซ่อนอยู่ในโค้ดที่แก้ไว้ก่อนหน้า

**อาการ:** หลังแก้ข้อมูลเสร็จ เปิดเว็บแล้วเจอ error เดิมเป๊ะจากข้อ 2 (`Access denied for user 'root'@'localhost'`) ทั้งที่ connection string ใน `appsettings.Development.json` ถูกต้องทุกอย่าง

**การไล่หาสาเหตุที่ผิดทาง:** ลองปิด SSL (`SslMode=None`), ลองอัปเกรด library `MySqlConnector` เป็นเวอร์ชันล่าสุด, ลองสร้างบัญชี MySQL ใหม่มาทดสอบแยก — ไม่มีอันไหนแก้ได้เลย เพราะกำลังไล่ผิดจุด

**สาเหตุที่แท้จริง:** ไฟล์ `Csi402dbContext.cs` มีเมทอด `OnConfiguring` ที่เขียนทับ connection string จริงด้วย password ปลอม (`YOUR_PASSWORD_HERE`) **แบบไม่มีเงื่อนไข** ทุกครั้งที่แอปเริ่มทำงาน — ไม่ว่า `appsettings.Development.json` จะตั้งค่าอะไรไว้ก็ไม่มีผล เพราะโค้ดส่วนนี้ทำงานทีหลังและเขียนทับเสมอ

ที่มาของบั๊ก: ตอนแก้ปัญหาข้อ 3 (เอารหัสผ่านจริงออกจากซอร์สโค้ดก่อน push ขึ้น git) ได้เปลี่ยนรหัสผ่านในไฟล์นี้เป็น placeholder แต่ลืมตรวจว่าโค้ดส่วนนี้จะ**ทับค่าจาก config เสมอ** ไม่ใช่แค่ใช้เป็น fallback ตอนไม่มีการตั้งค่าจาก DI

**วิธีแก้:** เพิ่มเงื่อนไข `if (!optionsBuilder.IsConfigured)` ครอบโค้ดนี้ไว้ ให้ใช้ค่าจริงจาก `appsettings` เป็นหลักเสมอ และใช้ placeholder เฉพาะตอนไม่มีการฉีด config มาจาก Dependency Injection เท่านั้น (เช่น กรณีใช้เครื่องมือ `dotnet ef` command line โดยตรง)

**บทเรียนสำคัญ:** การแก้ปัญหาความปลอดภัย (เอา secret ออกจากโค้ด) กับการแก้ปัญหาการเชื่อมต่อ เป็นคนละเรื่องกัน แก้อย่างหนึ่งอาจไปสร้างบั๊กอีกอย่างซ่อนไว้แบบไม่รู้ตัว ถ้าไม่ได้ทดสอบยืนยันผลจริงหลังแก้ทุกครั้ง

---

## สรุปภาพรวม

ปัญหาส่วนใหญ่ที่เจอไม่ใช่ "เขียนโค้ด feature ไม่ได้" แต่เป็นเรื่อง:
- **Environment/config เข้ากันไม่ได้** (MySQL เวอร์ชันใหม่ + authentication plugin)
- **ความสอดคล้องระหว่างเอกสารกับโค้ดจริง** ที่ค่อยๆ เพี้ยนไปหลังแก้ไขหลายรอบ
- **ความละเอียดของข้อมูลตัวอย่าง** ที่มักถูกมองข้ามจนกว่าจะต้อง demo จริง
- **ผลข้างเคียงของการแก้บั๊กหนึ่ง ที่ไปสร้างอีกบั๊กหนึ่งซ่อนไว้**

ทั้งหมดนี้คือสิ่งที่เจอได้จริงเวลาทำโปรเจกต์ไปจนถึงขั้นต้อง deploy/demo ใช้งานจริง ไม่ใช่แค่ทำให้ compile ผ่านอย่างเดียว
